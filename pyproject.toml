[project]
name = "brain"
version = "0.1.0"
description = "Brain: modular self-improving agent architecture — Phase 0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
